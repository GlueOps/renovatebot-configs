{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "docker:pinDigests",
    "helpers:pinGitHubActionDigests",
    ":pinDevDependencies"
  ],
  "separateMinorPatch": true,
  "separateMultipleMajor": true,
  "separateMultipleMinor": true,
  "labels": [
    "auto-update"
  ],
  "prConcurrentLimit": 0,
  "prHourlyLimit": 0,
   "gitIgnoredAuthors": ["github@glueops.dev", "vendors@glueops.dev"],
  "rebaseWhen": "behind-base-branch",
  "dependencyDashboardAutoclose": true,
  "dependencyDashboard": true,
  "recreateWhen": "always",
  "commitMessage": "{{#if depName}}{{#if isPatch}}chore(patch): update {{depName}} to v{{newVersion}} #patch{{else}}{{#if isPin}}chore(pin): update {{depName}} to v{{newDigestShort}} #patch{{else}}{{#if isLockfileUpdate}}chore(lockfile): update {{depName}}-lockfile #patch{{else}}{{#if isPinDigest}}chore(pinDigest): update {{depName}} to v{{newDigestShort}} #patch{{else}}{{#if isMinor}}feat: update {{depName}} to v{{newVersion}} #minor{{else}}{{#if isMajor}}BREAKING: The dependency `{{depName}}` has been updated to a new major version (v{{newVersion}}), which may include breaking changes. #major{{else}}chore(fallback): update {{depName}} to v{{newVersion}}{{/if}}{{/if}}{{/if}}{{/if}}{{/if}}{{/if}}{{else}}chore: Lock file maintenance{{/if}}",
  "lockFileMaintenance": { "enabled": true }
}
